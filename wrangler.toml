# ── Hibiki v12 · Cloudflare Pages ─────────────────────────────────────────────
name = "hibiki-v12"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

[build]
command = "npm run build"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
